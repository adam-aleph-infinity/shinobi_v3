/** Single source of truth for the UI version.
 *  Bump this before every commit that ships a user-visible change.
 *  Tag the commit with `git tag v{VERSION}` after each major release.
 *
 *  Versioning convention:
 *    MAJOR – large milestone / breaking change
 *    MINOR – new feature or significant improvement
 *    PATCH – bug fix or small tweak
 */
export const VERSION = "5.2.4";
