# Environment / secrets — NEVER commit these
.env
.env.crm
.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*
yarn-debug.log*
*.tsbuildinfo

# Database — persists on VM, never in git
ui/database/shinobi.db
ui/database/*.db

# Runtime data — persists on VM, never in git
ui/data/agents/
ui/data/index.json
ui/data/all_crm_agents_customers.json
ui/data/_persona_agents/*.json
ui/data/_fpa_generator_presets/*.json
ui/data/_fpa_scorer_presets/*.json
ui/data/_comparison_presets/*.json
ui/data/copilot/*.json
ui/data/copilot/sessions/*.json
ui/data/copilot/sub_agents/*.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Multi-agent worktrees (never commit these)
.trees/

# Agent orchestration DB (local state, not for git)
.agent-orchestration/
